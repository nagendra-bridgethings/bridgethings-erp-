name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

# Without this the default GITHUB_TOKEN is read-only and the Deploy step
# can't push to the gh-pages branch (403 error).
permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        # `npm install` (not `npm ci`) so the runner can heal small drifts
        # between the lockfile and package.json — npm ci is strict and was
        # failing on transitive @emnapi version pins that resolve differently
        # on Node 20 (CI) vs the local Node version.
        run: npm install

      - name: Build
        run: npm run build
        env:
          # Pass Supabase credentials at build time — Vite inlines anything
          # prefixed with VITE_ into the bundle. Set both as GitHub Actions
          # repository secrets (Settings → Secrets and variables → Actions).
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      # SPA-routing fix for GitHub Pages: copy index.html to 404.html so
      # any unknown path (e.g. /partner/orders refreshed) still loads the
      # React app, and the router renders the right page. Without this,
      # GitHub Pages would serve a real 404.
      - name: Copy index.html → 404.html for SPA routing
        run: cp dist/index.html dist/404.html

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
